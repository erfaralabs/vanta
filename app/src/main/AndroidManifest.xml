<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network & Hardware Vibrator -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Lets the user whitelist Vanta so the background telemetry sync runs reliably -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Foreground service keeps Vanta alive + visible under "Active apps".
         health type = no Android 15+ 6-hour dataSync timeout, correct for fitness.
         HIGH_SAMPLING_RATE_SENSORS is a normal (auto-granted) permission that
         satisfies the "any of" sensor requirement for the health FGS type on
         devices where Health Connect permissions aren't granted yet — without it
         the app crashes at launch on a fresh install. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- Push notifications (runtime-granted on Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Reschedules the daily 08:10 / 21:30 check-in alarms after a reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Health Connect Permissions (must EXACTLY match HealthConnectManager.permissions) -->
    <!-- Activity -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />

    <!-- Vitals -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />

    <!-- Health Connect Background Reads (Android 14+) -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />

    <application
        android:allowBackup="false"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vanta">

        <!-- Native vendor libraries for GPU / NPU hardware acceleration in LiteRT-LM -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />
        <uses-native-library
            android:name="libGLES_mali.so"
            android:required="false" />
        <uses-native-library
            android:name="libvndksupport.so"
            android:required="false" />
        <uses-native-library
            android:name="libneuralnetworks.so"
            android:required="false" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Vanta"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registered Health Connect Permission Rationale Alias -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <service
            android:name=".data.service.VantaBackgroundService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Daily 08:10 morning / 21:30 night template check-ins -->
        <receiver
            android:name=".data.notification.CheckInReceiver"
            android:exported="false" />

        <!-- Re-arms the check-in alarms after reboot / app update / time change -->
        <receiver
            android:name=".data.notification.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- 1x1 Quick Score Widget -->
        <receiver
            android:name=".widget.VantaQuickScoreWidget"
            android:exported="true"
            android:label="Vanta Quick Score (1x1)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.vanta.app.ACTION_CYCLE_1X1_METRIC" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/vanta_quick_score_widget_info" />
        </receiver>

        <!-- 2x1 Daily Summary Widget -->
        <receiver
            android:name=".widget.VantaDailySummaryWidget"
            android:exported="true"
            android:label="Vanta Daily Summary (2x1)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/vanta_daily_summary_widget_info" />
        </receiver>

        <!-- 2x2 Flagship VANTA Core Widget -->
        <receiver
            android:name=".widget.VantaCoreWidget"
            android:exported="true"
            android:label="VANTA Core (2x2)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/vanta_core_widget_info" />
        </receiver>

        <!-- FileProvider for secure camera photo capture & image sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
